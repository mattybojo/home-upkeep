import { ChecklistRowStatusDirective } from './checklist-row-status.directive';

describe('ChecklistRowStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ChecklistRowStatusDirective();
    expect(directive).toBeTruthy();
  });
});
