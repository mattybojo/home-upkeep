import { TaskListRowStatusDirective } from './task-list-row-status.directive';

describe('TaskListRowStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskListRowStatusDirective();
    expect(directive).toBeTruthy();
  });
});
